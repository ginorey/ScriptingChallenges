#!/bin/bash 
counter=0

while true; do
COUNTER=$(( counter +1))
ERROR_AMOUNT=$(($1 2>&1 > temp.txt)
if [$? -eq 1 ]; then
OUTPUT=$(cat temp.txt)
echo "It took $COUNTER runs to fail."
echo "Standard Output:"
echo $OUTPUT
echo "Standard Error:
rm temp.tx
break
fi
done
	
